[package]
name = "nms-video-overlay"
description = "Video overlay for No Man's Sky Cockpit Video Player"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "nms-video-overlay"
path = "src/main.rs"

[features]
default = []

[dependencies]
# Core ITK libraries
itk-protocol = { path = "../../../core/itk-protocol" }
itk-shmem = { path = "../../../core/itk-shmem" }
itk-ipc = { path = "../../../core/itk-ipc" }

# GUI
egui = { workspace = true }
egui-wgpu = { workspace = true }
egui-winit = { workspace = true }

# Async runtime
tokio = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Logging
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# Graphics - must match egui-wgpu 0.28's wgpu version
wgpu = { workspace = true }
winit = { workspace = true, features = ["rwh_06"] }
pollster = "0.3"
bytemuck = { version = "1.14", features = ["derive"] }
raw-window-handle = "0.6"

# CLI
clap = { workspace = true }

# Windows platform APIs
[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Input_KeyboardAndMouse",
] }

[lints]
workspace = true
